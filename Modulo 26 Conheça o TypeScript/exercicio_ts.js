"use strict";
// Simplificado
const Multiplicação = (Primeiro_Numero, Segundo_Numero) => Primeiro_Numero * Segundo_Numero;
const Saudação = (Nome) => `Olá ${Nome}!`;
// Padrão
function Multiplicacao(Primeiro_Numero, Segundo_Numero) {
    return Primeiro_Numero * Segundo_Numero;
}
function Saudacao(Nome) {
    return `Olá ${Nome}!`;
}
